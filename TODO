General
-------

- Fix recursion in encoder/decoder decorators

- More encodings

- Add error policies ("strict", "ignore"...)

- We can lookup encoder/decoder in client code by open database with
  'protected' flag?
